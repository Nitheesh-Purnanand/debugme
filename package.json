{
  "name": "debugme",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "build": "npm install --prefix backend --legacy-peer-deps && npm install --prefix authentication --legacy-peer-deps && npm run build --prefix authentication",
  "start": "npm run start --prefix backend",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
